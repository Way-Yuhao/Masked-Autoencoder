_target_: src.models.mae_pretrain_module.MAEPretrainLitModule

net:
  _target_: models_mae.mae_vit_large_patch16
  norm_pix_loss: false

optimizer:
  _target_: torch.optim.AdamW
  _partial_: true
  betas: [0.9, 0.95]

mask_ratio: 0.75
weight_decay: 0.05

lr: null  # to be determined at run time (scales with batch size)
blr: 1.0e-3
min_lr: 0.0
warmup_epochs: 40
accum_iter: 1

compile: false
